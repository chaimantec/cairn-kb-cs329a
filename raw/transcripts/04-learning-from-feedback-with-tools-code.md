---
title: Part 4 | Learning from Feedback with Tools/Code
lecture: 4
video: https://www.youtube.com/watch?v=Lxh9RF5S-K0
source: edited from auto-generated YouTube transcript
verbatim: false
original: original/04-learning-from-feedback-with-tools-code.md
---

# Part 4 | Learning from Feedback with Tools/Code — transcript

Copy-edited for punctuation, sentence boundaries and mis-heard terms. Terms were restored from context and cross-checked against the LaTeX/PDF text of the three papers this lecture discusses — ReAct: Synergizing Reasoning and Acting in Language Models (Yao et al., 2022, `2210.03629`), RLEF: Grounding Code LLMs in Execution Feedback with Reinforcement Learning (Gehring et al., 2024, `2410.02089`), and Constitutional AI: Harmlessness from AI Feedback (Bai et al., 2022, `2212.08073`) — plus the text layer of the CodeMonkeys PDF (`2501.14723`), a paper the lecture names in passing. The captions do not name the lecturer; they do name the course's co-instructor, restored below. Student questions and comments from the floor are set in *italics*. Timestamps mark the start of each paragraph; all 92 are preserved in order, and no sentence has been moved across a timestamp. [INAUDIBLE] markers from the original captions are kept as-is; the captions' low-confidence "[? word ?]" markers were resolved to the plain word where legible in context (brackets dropped), or flagged inline with [Ed: unclear — …] where not. The verbatim auto-generated captions are kept at `raw/transcripts/original/04-learning-from-feedback-with-tools-code.md`.

**This is an edited transcript.** Restored terms:
- *Azalia* ← "Azalea" (≈0:05, ≈40:28, ≈1:04:40) — course co-instructor Azalia Mirhoseini (course website); captions render her name phonetically
- *Claude Code* ← "Cloud Code" (≈28:03, ≈45:17) — restored from context (a coding-agent product, discussed here as something the class uses for engineering/coding tasks); "Claude" heard as "Cloud" is a common auto-caption homophone error, not a paper-specific term
- *CodeMonkeys* ← "Code Monks" (≈46:02) — confirmed in the CodeMonkeys PDF text layer (`2501.14723`), "CodeMonkeys: Scaling Test-Time Compute for Software Engineering," whose author list includes Azalia Mirhoseini (Stanford), matching "Azalia's lab" in the same sentence; not one of this lecture's three papers, but named in passing
- *hillclimb* ← "climate" (≈40:28, "to help climate") — restored from context; the lecturer uses "hillclimb" as a term twice elsewhere in this lecture (≈46:50, ≈47:36, "you can use that to hillclimb"), and "help climate" does not otherwise fit "you might have to make different set of choices to help hillclimb"
- *public test set* ← "public license" (≈32:40) — restored from context; the lecturer's reply ("It's a test set. You have a set of tests that you're keeping as public test...") makes clear the student asked about the public test set, not a "license"
- *context window* ← "comments window" (≈44:28) — restored from context; the discussion question at ≈43:41 is explicitly "the codebase doesn't fit the context window," which this student's proposed fix (fitting summaries "in the ___ window") answers
- *it* ← "you" (≈48:22, "ask it to revise its response") — restored from context; the surrounding description is entirely third-person about the model critiquing and revising itself, so "ask you" does not fit
- *self-critique and revision* ← "self-critiquing relations" (≈55:23) — confirmed against Constitutional AI's own "critique" and "revision" stage terminology (see ≈49:09–50:46 of this lecture and the paper text, e.g. "critique-revision pipeline")
- *LLMs* ← "elements" (≈1:03:06, twice, and ≈1:03:53) — restored from context; a student's question about "designing elements" / "cognitive behavioral science with elements" / "innovate elements like this" only makes sense as "LLMs," consistent with how the lecturer immediately answers ("the closed-source LLMs have done better...")
- *10@k* ← "10 at k" (≈35:49) — notation kept per this course's convention for `pass@k`-style notation (see lecture 3 transcript header, itself citing the lecture 1 header), matching the RLEF paper's own `n@k` solve-rate notation

**Unclear passages**, marked inline with [Ed: …]:
- "their [Ed: unclear — captions read "permission"] is very different depending on the task" (≈24:13) — the student's opening word before elaborating that humans rely on experience or step-by-step thinking depending on the task; "permission" does not fit, but no single replacement (e.g. "process," "cognition") is settled by context
- "a [Ed: unclear — captions read "Python"] fine-tuning with RLHF versus just [Ed: unclear …] fine-tuning" (≈46:50) — "Python fine-tuning" does not fit; words such as "plot" and "plain" are possible but not settled, so the methods compared on the chart the lecture shows are left unclear
- "[Ed: unclear — captions read "AI", marked low-confidence] is generally done as the last stage of the model" (≈52:18) — the captions flag the word themselves, and nothing in context settles which training stage is meant
- "much less harmless [Ed: …] as a result" (≈54:38) — kept as captioned; the next sentence says the harmlessness scores are much higher, so this is likely a slip for "more harmless", but the transcript does not change what was said
- "memories and [Ed: unclear — captions read "sketches" …]" (≈1:06:12) — a student's definition of an agent; the lecturer's reply ("doesn't have sessions, doesn't have memory") suggests "sessions", but the captions do not settle it

**Checked, not changed:** "GSPO" (≈32:40) does not appear in this lecture's three papers; it is kept as captioned. "HotpotQA," "FEVER," "WebShop," "PaLM," "CodeContests," "SWE-bench," "16 principles," "critique request"/"revision request," and the score pair "66.6"/"82.1" are already correct as captioned (confirmed against the ReAct, RLEF, and Constitutional AI sources above).

---

**[0:05]** In the first lecture, what we covered is that large language models are really good at solving natural language processing problems, and I think most of you are very familiar with using them in chatbot scenarios. But to make them useful in real-world tasks, they need to be able to interact with real-world environments, tools, and code, and learn from what they interact with. You already saw some examples of test-time compute and robust verification that Azalia covered in the last two lectures. Now we'll build a little bit more on top of that. Today's lecture, we will focus on three different papers. The first paper will start with the notion of tool calling — how can we get the models to go beyond just reasoning

**[0:50]** to also take into account actions via tool calls? The paper that we'll cover here is called ReAct, which was actually one of the first papers to combine reasoning and acting in language models. The second paper that we'll look into is RLEF, or Grounding Code LLMs in Execution Feedback. What this focuses on is how you actually do code generation while using execution feedback from being able to run the code, or run the tests for the code. And the third thing that we'll look into is Constitutional AI, where we use the model critiquing itself as a feedback loop to improve the model. So all three of those techniques are ways for the model to improve itself. But what differs in each of these techniques

**[1:36]** is where the feedback is coming from. It's some form of interaction with either the environment, or some form of critique mechanism, or grounding using what we know should be the right answer. So let's start with tool calling as the first abstraction. What ReAct really tried to focus on was this notion of, when humans want to go and do some action, they think about it first. For example, if you decide that you want to come take a class, you think about how that might actually help you. And what this roughly means is that, once you have thought about it, then you will have a reason to act. And then based on that action, you

**[2:22]** might observe that you have a new observation about the environment. So once you come to the class, you might decide you like the class or you don't like the class. And based on that information, you come up with new reasoning. Maybe you'll go do the homework, and then that will make you decide something else. So there is this loop between thinking and action that helps us function as humans. A similar loop can be very useful in terms of refining what language models generate. Typically, when you look at language models, they have struggled to combine reasoning and acting by themselves as isolated processes. In today's models, the reasoning has become innate, because thinking has become part of the models. But if you look at the history of LLMs in the last couple of years, this was not something that came out of the box.

**[3:09]** One of the challenges — why it was hard for models to combine reasoning and acting — is that they typically hallucinate. They have no context of what is actually happening in their environment. So, for example, if you were to type into a model like Gemini or ChatGPT or Claude, "What is the temperature today?" or "Where is something happening today?" it has no information about current events, unless it actually goes and does a search call and then fetches that information. So it has no notion of grounding in the real world. And as a result of that, it cannot go and take actions, and then actually combine that back into reasoning. ReAct was one of the first abstractions that tried to combine it in a way that would be effective.

**[3:54]** And what you see today's models do is actually go beyond ReAct to do this within the chain of thought itself. Let's take a look at what we covered in lecture 1. If you look at chain of thought by itself, chain of thought already gives the model some sense of reasoning. So if you gave it a math problem, the model will show the steps, the intermediate steps it takes to get to the answer, but these steps may or may not be grounded, because they are based on the model's internal state. They have no feedback from the external world. And then there is a second class of LLMs that were developed — for example, WebGPT — which go and interact with, say, web browsers. And what they're basically learning is how to interact with web browsers, which is a form of a tool. So they don't necessarily have a reasoning process,

**[4:40]** but they're basically learning how to interact with web browsers. So you basically can fine-tune on interaction traces with web browsers, and then on what humans prefer. But as a result of those two different things — one focused more on reasoning, one focused more on actions — how do we combine these two paradigms where we can both reason and act in a useful way? A very simple abstraction that ReAct came up with was just using prompting. You can get the model to generate verbal reasoning traces. So first, you say, OK, I want the model to go do this task, and ask the model to think, say, step by step about it, and then it comes back with its reasoning trace. You append that in the prompt, and then you say, OK, what action should be taken based on that?

**[5:25]** And then you go take an action. This really helps the model improve performance where grounding matters. So HotpotQA, for example, is a question-answering task. Then FEVER is a fact-checking task. These require real-world knowledge — they require knowledge about what is happening in the world. And then WebShop, for example, is a task where you're supposed to ask the model to go buy products on the internet. So it's supposed to do a series of tool calls to actually identify what product to search, and then have a sequence of those actions to actually complete the purchase for the user. Each of these tasks requires some notion of grounding in the real world, and without that grounding, it's not possible for the model to keep making progress. A second benefit of having the model explicitly do reasoning and action

**[6:12]** steps is that it will allow the model to give traces that are more interpretable, and interpretable traces allow humans to really trust the model's responses. It's almost like getting the model to break down what it's doing step by step. So what you might be familiar with is that, if you prompt the LLM and ask it to think step by step, which we were showing in earlier examples, then you will get some form of a reasoning trace where it shows its intermediate steps. And then, if you ask the model to, say, do a tool call — go search, for example, Google, or find the temperature — it will go call an API, and it will get a bunch of observations. So that would be action only. Typically, you will have a list of actions if you have a list of tool calls that are valid.

**[7:00]** What ReAct does is it will take a goal and have some form of reasoning — so that will be the left loop. And then, based on those reasoning chains, it will go and act — that will be the loop on the right. So what this is setting up is a full agent in itself, where you have some observations about the world and then some set of actions based on what the context is. And if you were to, for example, take this entire context with observations and the set of actions that have happened in the past, typically you could go learn a policy, but that would require a full loop, which would be complex and expensive. But you can actually start doing this in language space, which is what ReAct is doing, where it's basically just generating the thoughts in language space, and those thoughts are not affecting the environment.

**[7:47]** And then, based on those thoughts, it's then going and taking an action, just in the prompting space. So it's a very simple abstraction of how to prompt the models to start thinking about these things and generate actions which are valid. One interesting concept I'll mention here is that, when you get the models to generate actions in this particular slide, typically, how do you know that these actions are valid? One typical way in which you get these models to generate correct actions is to frame the problem more as a classification task. You say that this is the valid subset of actions, and then use that as a way to say, OK, here is your reasoning, here is the state of the world, and here are the valid actions to take next.

**[8:32]** And then based on that, it goes and decides, OK, this is the action to take, and then that generally provides a more grounded response. And in fact, this was also used in another paper that we worked on in collaboration at Google with robotics folks, where the state of the world and all of that requires very valid actions — you can't really operate without having a list of actions. So that provides one way to really ground the knowledge of what actions to take next. In terms of implementing this idea in a very simple way — this is an older model now — this was done with the PaLM model, a frozen LLM. In few-shot examples, the model was given some actions, some thoughts, and some observations, and then asked to go take the next action.

**[9:18]** So for the reasoning traces, it was basically seeing actions and thoughts. I'll show you an example. And for the decision tasks, it was basically looking at what's the state of the world, what is the thinking, and then deciding whether to reason or whether to take action next. So you can either go and reason, or you can decide whether I have to reason or take action next. Let's look through an example and walk through it carefully. Aside from the Apple Remote, what other device can control the program the Apple Remote was originally designed to interact with? This is basically a question from HotpotQA. If you basically just do standard prompting, the answer that will come back will not be correct. Now, you can do chain of thought, where you basically

**[10:04]** ask the model to go think step by step. It's showing you all the intermediate steps here. However, the answer is still not coming out to be completely correct. Now, let's see — if we ask it to take some actions where it can do some grounded knowledge, and it can do some search calls, it goes and sees that, OK, the thing that I'm really needing more information on is Apple Remote. So it goes and does an action on Apple Remote. It gets some observation. Then it goes and does another search call on Front Row, which is some text that is there in observation 1, and then continues to follow on. And then it realizes that, OK, this is a discontinued something. So as a result, there is not really a good answer that it can give.

**[10:51]** Now, how does interleaving thinking and action in those traces help? So here, it thinks first that, OK, I have to search for Apple Remote, and then find the program it was originally designed to interact with. So it first goes and searches Apple Remote. It finds the information that it's a remote control introduced very early on — 2005, two decades ago — and it was originally designed to control the Front Row media center. Then it thinks, OK, so I need to search Front Row next. So it goes and searches for this. It's not able to find it. So then it comes up with a new terminology called Front Row software. It searches that, and it's able to figure out, OK, it's a discontinued media software center. And then it thinks about it again, so it's able to give an answer based on that.

**[11:39]** So basically, the reasoning process is an abstraction which allows it to come back with a better answer than just taking actions and then coming back with answers in between. Interleaving the thought and action and the observation aspect is basically allowing the model to behave better. Now, what is happening in LLMs today — for example, if you go into the thinking mode of, say, Qwen or other models, you will see this happening automatically. So if you actually turn on the thinking mode, say, in Qwen models, you will see all of this starting to happen, because the models have already been distilled on these traces. So the thinking-fusion stuff that happens in several of the open-source models already captures these things — they've already learned how to do, for example, tool calls and so on. Questions?

**[12:27]** Yes. *How does the model know what it knows — what to search, or when to search? Because clearly, you don't want to search on 1 plus 1. But how do you determine the search? How do you determine--* I mean, here it's searching, and then it's basically coming up with — here, it's getting a feedback. It doesn't know what it knows. This is from interaction. *But you already have been trained on the Front Row Wikipedia page. You shouldn't need to search.* Yes, it doesn't need to search. Yes. *But does the model know whether it knows this?* So I think there's a contradictory set of opinions on whether the model knows what it knows. There is one set of people that will tell you that models are very confident in knowing what they know.

**[13:13]** But I think the other side is that, typically, if you were to ask the model to rate its output — are you confident about an output? — typically the models are overconfident. They're not well-calibrated. I mean, that's a research problem that is still being solved. So as someone who's designing an application, what you are looking for is not so much knowing whether the model knows what it knows, but getting the model to use the right set of tools so that you have grounded knowledge. Does that make sense? *Yes. Yes. With the ReAct on the right, when you're saying with the left loop you elicit the reasoning to then append to original knowledge — will it produce the reasoning of the one, or two, or three, or four initially, and then it goes and just acts*

**[14:03]** *for each one, or does the act influence the next reasoning step?* It's interleaved, so it's thought one, then act one. So it's happening in an interleaved fashion — thought one, act one, thought two, act two. Each one is basically informing the next. It's very much like the human process: you're thirsty, so you basically start walking towards the kitchen. You get to the kitchen, maybe water is out, so then you decide the next step based on that — you think, maybe I have to go to the supermarket to do something. So it's very sequential in that respect. You can do parallel sampling and all of those tricks, but then you have to explicitly design that process.

**[14:49]** Yes. *There's no guarantee as to a search result. [INAUDIBLE]. So what do you [INAUDIBLE] observe [INAUDIBLE] is the search result?* That's a great question. If there's a contradiction between the results, then what do you want the-- I think this is a question that the user has to decide, if there's not enough consensus. So typically, one of the things that we covered in the second class was that we can get more confidence by doing, say, majority voting. You basically say, OK — or you have some way of validating the answer before you give it to the user. So for the end application, what you care about is that the answer is valid. So you need more guardrails.

**[15:38]** But hallucination with search results can be controlled much better than just depending on the internal state of the model. Other questions? Yes. *Maybe this is not answerable, but also [INAUDIBLE] from the-- I wonder if you have looked at the KV-cache representation of the thought versus the observation, and see if there is any sort of interleaving pattern between them, given that the observations are generally given by the environment — they are not generated by the LLM itself. So I wonder if there would be these changes in between.* That might be your research project for this class. There are open-source LLMs that you can work with.

**[16:24]** Yes. *It's just a little termination, like technology [INAUDIBLE], but I know this is like the action space of ReAct, from where it's the actions plus thought source. Why do we just think of thoughts as internal to the policy model, and maybe the action is just like search or finish? Why do we say thought is also part of the action space?* We're not calling it part of the action space. We are calling it a separate thing — actions are on the right loop, and the reasoning traces are on the left loop. Are you talking about this one? So I think because the language models are trained on language,

**[17:10]** typically they benefit from having the reasoning tokens in the right abstraction as a way to output the right action. That's the main reason to have those tokens. I mean, if you had intermediate representations, then maybe it doesn't matter. So let's take a look at some results. These two tasks — HotpotQA is a multi-hop question-answering task over Wikipedia, and then FEVER is more of a fact-checking task. The action space in these two tasks is very much focused on, say, search over Wikipedia pages, or looking up a certain string, or finishing — "OK, I finished whatever task I was given." The action space is actually very simple. It's basically simulating human interaction with Wikipedia

**[17:56]** here. So the baselines here are simple, where you're basically building a prompt. The standard prompting that you will see is simple prompting — no thoughts, no actions, no observations. Chain of thought, where you're basically asking the model to think step by step. Chain of Thought self-consistency — SC stands for self-consistency — where you're basically doing majority voting, or self-consistency; both of those terminologies are exactly the same thing. And then Act refers to there being no thinking in between — you're basically just taking actions. And there are two variants you will see in the table, where you can fall back to Chain of Thought self-consistency if ReAct fails after a certain number of steps, and then this: if the majority answer occurs less than half the time, then it can back off to ReAct. So you'll see this in the results.

**[18:41]** So this is an example of when they prompted this large model. What they found was that ReAct generally does better than action only. It did not always outperform chain of thought, which was interesting — it outperformed chain of thought in the case of FEVER, but in the case of HotpotQA it did not always outperform. But if you combined this chain of thought, self-consistency with either fallback or the other way around, then you do actually outperform the standard techniques. So what that roughly tells you is that there is value in properly combining the model's internal knowledge with external knowledge, and then using reasoning as an intermediate way for the model to arrive at the right answer. So basically, that allows the model to reason about what is the right and accurate information

**[19:28]** to retrieve here. And it's basically showing that it's able to use search as a tool to retrieve the right information. And then another interesting aspect here is — what this is saying is, when it's successful, what is happening? Did it get the correct reasoning traces? And when it's failing, what errors are happening? So one of the things that I want to highlight in this slide, without going through too much detail, is that in chain of thought, hallucination ends up being a major problem, a major failure mode. But in ReAct, you basically end up having grounded information, and the model is more trustworthy because it has access to this external knowledge base. So it allows it to make fewer errors, because it can successfully retrieve the knowledge via search.

**[20:15]** And then there is some difference between prompting versus fine-tuning — when you can fine-tune, ReAct definitely does better. And then, if you can do an outer loop, then it does even better, which we'll cover next week. So that was knowledge-intensive tasks. If you were to look at decision-making tasks — and this can be, say, online web browser experiments, or robotics experiments, or gaming environments — where we will look at WebShop as an example, you require the agent to purchase a product based on user instructions. For example, you're asking the model to look for a nightstand with drawers, and then you're basically evaluating whether the model gets to success when it's interacting in this environment. It's a simulated environment. And then you compare it as a baseline to imitation learning.

**[21:00]** That's just fine-tuning. And then imitation learning plus RL, which is basically just supervised fine-tuning plus RL. And the results that you see here are impressive, in the sense that even compared to, say, imitation learning or imitation learning plus RL, having the reasoning abstraction with ReAct, you get a higher score, and you get higher success rates compared to what the baseline gets here. So the score refers to immediate steps, and the success rate refers to getting to the final — getting success on the action, sorry, the task — that the user had assigned in this particular case. So ReAct generally outperforms what was possible with just imitation learning or supervised fine-tuning before. But it's still far below what human experts can do.

**[21:47]** There's still a lot of headroom compared to, say, human experts, as you can see. ReAct just gets 66.6 versus human experts, who get 82.1, in this particular case. And a lot of the challenge shows up in success rate, which is lower, because if you get any of the steps wrong — since this is a multistep process — then the errors cascade over time. So we covered ReAct first. It's basically a simple method that allows you to combine actions and thoughts. But one of the challenges with this particular approach is that, if you have very large action spaces, then you need a lot more demonstrations than can fit in context. And since you require multiple reasoning steps, your inference costs will be higher.

**[22:34]** But overall, it's generally shown to have better performance in question answering, fact-checking, and decision tasks. And your hallucinations are lower, and you get better decision traces. So let's have a few minutes of discussion questions. What I will do is ask the class to think through these questions and talk to the person next to you, and then we'll come back in two minutes. Let's start with the last question. Any takers for the last question? By show of hands, any takers for the last question, to summarize what you guys discussed? What happens if the environment is noisy — if the feedback that you're getting is misleading?

**[23:27]** You can make it up on the go if you haven't discussed this one. OK. *So if the environment's feedback is noisy or incorrect, you can add another layer of reflection — that's the agent reflecting on what the environment gives you and then reasoning about that [INAUDIBLE].* OK. Yeah. *I was thinking maybe it's important for the agent to have an ability to backtrack whenever something doesn't make sense, or if it's going through a chain of thought that doesn't lead anywhere — just being able to backtrack.* Backtracking becomes important because the noisy feedback might lead it down repetitive loops. Yeah. *Or you could do external retrieval several times*

**[24:13]** *and [INAUDIBLE].* OK. Yeah. *So — better confidence metrics. You need some way to build confidence over things. You could have it perform the same task multiple times and take the highest sample [INAUDIBLE].* That makes sense — basically, you have a better understanding of how confident the model is in this environment's feedback. OK, good. Any ideas on the second one? So ReAct is one way you can think about reasoning and actions — what other cognitive mechanisms should we take into account if we are building these processes? Yes. *I mean, humans — their [Ed: unclear — captions read "permission"] is very different depending on the task.*

**[24:58]** *Sometimes we rely on our experiences in the past. Sometimes we think through things step by step. So it depends on the task at hand that we do that. Just relying on ReAct and taking an action doesn't necessarily lead us to the right place — sometimes you have to actually think and analyze and do reasoning before we actually do any actions.* So I think what you're roughly saying is that there are other thinking mechanisms that perhaps are worth exploring. One of them, what I would phrase as task decomposition — maybe the task is too complex, so maybe I should decompose it into subtasks before attacking it with these approaches. Or maybe there is something to be said about having parallel approaches to thinking, which

**[25:44]** someone had brought up here as well, as to — is this approach better, or is this approach better? And then having-- *Multiple memory, past experiences.* --have memory, past experiences. So memory is another aspect. OK. Yes. *I think humans also try to fine-tune what is the ideal ratio for reasoning to action. For some tasks, you might learn over time that it's better to think, give it much more reasoning time before acting it out. And it might be interesting to see if the paper actually tries to train this on different tasks, to see what is the ideal outcome, to compute a ratio for each task.* That's a great point. In fact, related to that, you'll find that some of the models

**[26:32]** today overthink, because they have really long thinking traces, and — yes, even for simple tasks, you'll see them have very long thinking traces. So there's definitely some interesting set of work there on how to get the models to not think too much. Let's go there. Yeah. *Another interesting direction would be, given the different model, the number of parameters, maybe some model is better at being able to do the whole tool calling or chain of reasoning or even delegating tasks. So being able to do that based on what models you're working with, supporting those different tasks.*

**[27:18]** *Almost like building a compound system where you benchmark subtasks on different models and then delegate based on the strengths of the model.* OK. That might be another project idea. Cool. OK, let's move to the second paper now. This one is called RLEF. It's based on coding agents, and what it's basically saying is that, when you're building coding agents, one of the things that you want to do is give some feedback for the reinforcement learning loop. And oftentimes, what really helps is execution feedback, where we can execute and get that feedback. So RLEF was one of the first papers that showed that, with execution feedback, you can actually get much better performance in coding LLMs. Why do we care about this problem?

**[28:03]** So how many of you use Claude Code today? OK, that's almost the majority of the class here. So I don't think I need to explain that a lot of the engineering and coding tasks seem to have been delegated to these agents. And the aspect that matters more in building these coding agents to be extremely strong is that we need to be able to understand the user intent, and, based on what the code was generated, we need some feedback from the code generated so that there is a way to iterate on top of that. So what this paper shows is basically an end-to-end RL fine-tuning framework. The actions, in this particular case, are generated code, and the observations that I was showing earlier come from execution feedback.

**[28:49]** Execution feedback is test feedback — you're running some tests, and you're collecting the output of whether those tests pass or fail, and you get a binary reward based on whether those tests pass or fail. Based on that feedback, you can iteratively refine, and at fine-tuning time, decide to make the model better based on that. So PPO can be used for this approach. The two techniques that this paper covers are a two-tier test strategy and a hybrid token/turn-level policy. Both of those are very interesting, so I'll cover the basic ideas here, but I do encourage you to read this paper very, very carefully. The core of their framework is an iterative feedback loop — they're using both training-time and inference-time execution feedback. So let me explain the training-time

**[29:36]** and the inference-time feedback. At the very top, in the purple block, what you're seeing is that the model gets a natural language problem description — for example, someone asked you to, say, write a Hello World program. Then it generates a code solution, which then evaluates on some public set of tests. If the code fails, the execution feedback is provided to the LLM for another attempt, and this cycle will continue until either the code passes, or it reaches a limit on the number of turns it's supposed to do this loop for. And then, based on this, whatever set of solutions it has generated for passing solutions, a private set of tests will determine what reward to give, and that will be used in the PPO training loop with RL.

**[30:22]** So what's happening here is there are two fundamental phases. This is the exploitation phase of RL, where it's basically exploiting the current policy through inference-time feedback loops, using whatever policy model you've got. And on the right side, you have an update of the policy based on the execution results, and this allows continuous improvement for the model to self-improve here. So let's take a look at how this works. You're given a simple example where you're creating code to detect, say, palindrome substrings — that's what the problem statement at the top is. In the first turn, it generates a basic solution, but the implementation actually fails on the public tests, and there's an execution timeout, which is a very common issue.

**[31:08]** After receiving this feedback, the model actually generates a better solution, where it does some optimization, and now it does pass the public tests based on that feedback. So you see that yellow block now submits the solution to the private set of tests, and based on that, it will go and try to improve the model, now that the solution is looking better. One of the key aspects of their approach was that, during iteration, the public tests provide immediate feedback and help it develop a better solution. But this is a small subset, for faster iteration, and this is basically allowing the model to guide itself to a better solution, while the private tests are completely hidden during the generation process.

**[31:55]** So basically, this separation allows it to not train on the public test, but only use the private test as the way to get signal. And this is one way in which the model cannot simply memorize the test outputs, because it's getting the execution feedback. So this, they found, is a very useful innovation in the self-improvement loop. A second innovation — so I will actually not make you go through the details of this RL algorithm; this is basically PPO. But the one thing I want to stress is that, when you're in a language model space, you're outputting tokens — you're outputting one token at a time. In the policy model, they are generating code tokens token by token, so this gives you finer control over the generation process. But when they're using the value function,

**[32:40]** they're computing this at the turn level. So this evaluation, or the reward, is happening over the entire response, and it's using the last token of the prompt — so there's a single advantage value for all tokens. So if you were to match this to where we are in, say, RL algorithms, this is closer to what GSPO would do, in terms of giving rewards to the entire sequence as opposed to doing it per token. Yes. *I didn't understand how do you — where do you get the public test set?* It's a test set. You have a set of tests that you're keeping as public test, and another set that you're keeping as private test. Say your question again.

**[33:28]** *So on the left, you said that's part of the difference — the user can give all sorts of program descriptions [INAUDIBLE].* Then this is for the outer loop. So, for example, CodeContests — there is a set of tests that are combined with CodeContests. You have to design the outer loop for the model to get better. You can keep some subset of tests as public tests, on which you will do the inference-time feedback, and then some subset of tests you can keep to train the model. *Oh, I see. Thank you.* Yes. *What does it mean for the test to fail?* When the test is failing, that's basically saying that the solution that the model generated is not correct.

**[34:14]** *The model itself evaluates that — the correctness? Or who evaluates the correctness of--* The test output is fed to the model: "You have failed," and then that's the execution feedback that's going into the LLM. You're solving this problem, this is the output you generated, and this is the feedback you got. This is shown in the example that I've shown here — this was the first thing, this code solution, and then the public test failed, so you're appending all of this. And then this is the execution feedback, and then you're asking it to generate the second solution. *I was just wondering, what's the evaluation process of a fail or pass?* That's an actual running of a test. It's a Python test — it's a simple Python function.

**[35:02]** In your homework, you will actually — not for code, but for math, for any problems, you'll actually try some of this out — not execution feedback, but just identifying the error in the generated solution. So why does this help, or what's the benefit of doing this? One of the things that this paper showed was that the y-axis is showing solve rates, and on the x-axis, it's showing the sampling budget, for two different sets — a validation set and a test set. And even though the results are a little bit older, because they're on Llama 3.1, they clearly showed that after RLEF, and training on CodeContests, which

**[35:49]** is competitive coding problems, you clearly get much better results in terms of solve rate. Basically, solve rate 10@k means that you're passing at least one out of — you have 10 solutions generated, and then you're passing a certain number out of that. So the solve rate definitely goes up with RLEF. The other thing to note here is that this is log scale on the x-axis. So this is definitely improving with the execution feedback. Now, why does it help is the other good question to ask. The base models typically don't benefit from access to just faulty solutions and execution feedback. What is really helping here is the fact that, when we are giving — when we are training on CodeContests,

**[36:35]** we are showing it, in each turn, what's the execution feedback, and with that you're basically also generalizing to all the other benchmarks. So it's the error in the solution that's what's helping the model start to get better. This becomes really clear in this particular example, where what they're looking at is the number of errors in turn 1, the number of errors in turn 2, the number of errors in turn 3, and then the number of code changes being made and the error type — this is for a smaller model, and this is for a larger model. So what you basically find is that, with RLEF, as you go iteratively through turn 1 and turn 2 and turn 3, you have fewer wrong outputs.

**[37:21]** And in subsequent turns, you basically start to repair the output. If you didn't have this iteration loop, then you basically are not making edits that are correct. So effectively, you are leveraging both the fact that you have higher diversity, in terms of the number of samples that you're making, but also your edits are more targeted, because you can look at where you made the error. That's one idea that you will use in your homework problem as well. *So with the execution feedback, does it also include the faults you made, the errors? Does it include any suggestions for how to fix it?* It's just getting a sense of — this is the part that gave an error.

**[38:09]** But these problems are very simple. The [INAUDIBLE] problems are still, I would say, not more than 100 lines of code, so they have much smaller problems. Yes. *How is the binary feedback enough to create the-- I have a feeling that maybe these were very easy problems, so that binary feedback was enough. But for a harder problem, you would probably also need to have the error trace or other metadata to actually know how to debug it more efficiently.* That's quite possible. I think that might make a very good project as well. Yeah. *Especially in [INAUDIBLE], accuracy at the first time.*

**[38:57]** *I feel like reward is a binary reward for the final solution. It means encouraging the model to just repair and repair [INAUDIBLE] all together [INAUDIBLE]. That's impossible [INAUDIBLE].* What's the question there? Oh, sorry. *So, looking at the reward — is it just binary reward, correct or incorrect? But the model has a freedom to do that in model. How does this RL method encourage the model to get it correct — get the problem correct — the first time?* So I think it really depends on the error it made. And I agree with you that, for more complex problems, as was brought up, it is possible that it will take multiple turns.

**[39:43]** But if it was a simple enough problem, then-- I mean, if you remember, there's inference-time feedback here also. You're appending what the error that it saw was, and you're allowing the public test to pass before you send it to PPO. So, to some extent, it will have had a few chances to go correct itself. *You can always increase the number of turns to finish the problem — like any reward, you think that--* Yeah, you can. You can try all of those things. I think these are-- I mean, this is one abstraction of that problem that I'm showing you. Overall, I think what we want to learn out of here is that the self-improvement loop works — that's the first thing that we're learning, and it can work in simple enough problems with binary reward.

**[40:28]** And then, potentially, I think the question is, between process reward models versus outcome reward models, which works better? I mean, what you're saying is, do we need to give feedback at every single step? And it's possible that that is what matters — Azalia covered it in the last class, the outcome versus process reward model. So I think that debate is not completely solved, in terms of — in each domain and each benchmark, you might have to make a different set of choices to help hillclimb. Yes. *I might have missed this, but why does it encourage generalization? Because I think — isn't the context more difficult than human level, and [INAUDIBLE]? And that might be why it generalizes better. And another thing is, what about supervised fine-tuning —*

**[41:16]** *would it be able to beat [INAUDIBLE]? For example, [INAUDIBLE]. Does the supervised fine-tuning model, or what, always matters?* Supervised fine-tuning on reasoning traces might actually still get you some of the gains, as you probably have papers out there saying that. But I mean, this is still a question up for debate. I think with RL, you are able to solve slightly newer problems, so there is a little bit more generalization that's seen. But with supervised fine-tuning, anything that's within domain will definitely start to see value — it's still the same loss function. *Does this have an ablation of the two-tiered task strategy? Because one way is you can just have all the tests in public,*

**[42:04]** *and then run this loop, and then at the end, you do show.* I see — so why did they do that? *What do you do with the [INAUDIBLE]? On the final result after the [INAUDIBLE]?* I see. I didn't see that ablation. I think there must be, well, some amount of — just, like, they wanted to use more fine-grained feedback than just the feedback of the public test, so that there is no leakage between the two sides. *Where would you care about leakage? Why would you not care about the leakage? I mean, they split the test into public and private themselves — it's all test, it's not in the training part of the model. So why do you have to split it into two tiers [INAUDIBLE]?* I mean, in the outer loop, if you're training on the thing that you're basically going to use as the feedback, then that aspect matters.

**[42:54]** We can take this one offline. *OK, sure.* I think there's a little bit of a terminology gap around this. I see. Yes. *In the ablation study, I was curious why RLEF produces fewer wrong outputs, but more timeout errors.* Oh, because it's probably-- so it's pure wrong outputs, because it's able to fix itself. But then, if it ran out of time in [INAUDIBLE], basically the tests ran out of time because the solution was still not correct. A lot of this is very domain-specific, also. OK, I'm actually going to skip this slide.

**[43:41]** But I think the main thing that I want you folks to take away is that there is a way to incorporate execution feedback to improve code generation as a domain. Competitive programming tasks is one place where it has shown promise, and it does generalize to other benchmarks in the code generation realm. So let's take a look at, say, question 2, and discuss that for one minute among your peers, and then we'll come back together as a class. Any takers for this problem? *The codebase doesn't fit the context window.* So one thing I know is that some tools, what they'll do is they'll do something similar to the previous paper we're

**[44:28]** *discussing, and they'll think about what information they need. They'll have access to some search tools, and then they will continue iterating on that until, at some point, they decide they have enough information, and then they move on to something like what we are discussing.* That definitely makes sense. Any other takers? Yes. *I think you would make this — the code of each element [INAUDIBLE] for summary, so that [INAUDIBLE] be something [INAUDIBLE], and then together they can fit in the context window.* Any other ideas? Yes. *I don't know if this applies here, but in a regular setting,*

**[45:17]** *I feel like you can build a graph-regularized version of your codebase, and then do a similarity based on what you can find, and again do another iteration on that, just simple [INAUDIBLE]. And then you can have in the loop what you said about verifying, and see if you have enough information. And then, if not, you can do that, because in that case you can use the summarization — you can use the things that you're extracting and then come up with the answer.* So this is actually the kind of thing that actually runs in Claude Code — it has to go search for what is relevant and then actually apply the code, and it's there. One of the benchmarks that targets this is SWE-bench.

**[46:02]** So all the ideas that you folks have proposed, in the realm of searching for something, having some summary, doing some representation — all of those are perhaps the first step to solving that problem, before you now do the test of what code patch to apply, and then whether it has passed or not. So the CodeMonkeys paper from Azalia's lab also targets very similar ideas here. Let's move to the last topic. The last topic is Constitutional AI, where we'll have the model learn from AI feedback. And the thing that we are improving about the model is harmlessness, or its ability to generate harmless outputs. So we want it to be helpful, but we also want it to be harmless.

**[46:50]** So what happened with LLMs, when they are base models, is that you can give them feedback in a lot of ways. A typical way to give feedback, when you are trying to build a chatbot, would be that you look at the outputs and you ask humans to rank them: is the model output correct? So you show it two different outputs, and you ask whether it's correct, whether it's useful, and whether it's specific enough. Based on those human preferences, you can build a reward model, and then you can use that to hillclimb. So here is a [Ed: unclear — captions read "Python"] fine-tuning with RLHF versus just [Ed: unclear — captions read "Python"] fine-tuning. And as you increase the model size, you clearly see better responses with RLHF. So basically, that's this whole notion of — you can use these preference responses

**[47:36]** from humans, as human feedback, to hillclimb on the model. Now, that doesn't scale very well, because if you have to collect tens of thousands of human labels, that's extremely time consuming and tedious — imagine that you generate all these model outputs, and then you have to ask humans to rate them. So one of the very interesting principles, given that the models can reason, was to use this notion of "constitutional" that Anthropic came up with, which was very much based on human-written principles. They basically used Constitutional AI, or human-written principles called the Constitution, and then used them to improve the model by describing what the desired behavior is. So humans don't need to be in the loop, except to write that Constitution. And the reason this notion of rules works is because the models get better at instruction following.

**[48:22]** So if you ask the model to format the response in a certain way, the model is able to follow that instruction. Or if you ask the model whether a response has this particular behavior, then the model is able to actually answer that in truthful ways. That's what the Constitutional AI aspect will be able to exploit. So what Constitutional AI did was it came up with a set of 16 principles, which they call the Constitution, and those will define the model's behavior. So these are going to be the set of prompts which will elicit whether it's following the Constitution or not, and then use that to critique the model itself — I'll show you an example — and then ask it to revise its response. So you're basically saying whether-- we're basically using some red-teaming prompts.

**[49:09]** You're looking at the model output, you're critiquing it, you're getting a revision, and then you're using that to fine-tune the model — and that's the supervised fine-tuning stage for Constitutional AI. And then this is the RL stage, where you're basically doing the same set of responses, and then using AI feedback to train a preference model, and then using that to train the final model. I'll show that in detail in just a second. So before we go there, let's take a look at what might be an example Constitution — I will not read this word by word, but I'll show you the basic examples. So the critique request, in this particular box diagram that I showed you, is that there is a red-teaming prompt, the model has an output, and then the critique request is basically asking if the model output has something harmful or unethical

**[49:58]** and so on. And the revision request is basically asking it to remove any of those responses. A second critique request might be, does it have any gender bias, and the argument about why that might be having a gender bias. And then the revision request is, can you remove any trace of that? And the third one is more like, is it inappropriate for young children, and then what would it take for it to be appropriate? And then the revision request is, rewrite it. So if you look at each of these, it requires the model to be able to identify these behaviors, and the second thing it requires is to be able to follow the instruction of now rewriting it with this particular style or way, or removing this content. And the way this loop works is that humans come in and set

**[50:46]** right, a set of principles that will be used for the self-improvement loop. In the supervised learning stage, you're basically fine-tuning the model on this data generated by the self-critique and revision stage. So the critique request, as I showed you, might be something harmful, something unethical, and the revision might be, OK, remove anything that's harmful and unethical. And just with supervised learning — with a number of turns you are able to — well, just with the number of revisions, the harmlessness improves and the helpfulness will decline if you just keep increasing supervised fine-tuning. But overall, the helpfulness plus harmlessness will improve monotonically, because if you're

**[51:32]** trying to get the model to a certain set of fixed principles, then it does become less helpful, is what they're saying. But overall, it's more helpful and more harmless together, is their claim. And then, in terms of the reinforcement learning stage: first, they train a preference model based on the responses in step 1 and the Constitution, and then they fine-tune the LLM to maximize over this preference model. So they're basically getting the model to respond in a way that's most thoughtful, respectful, and cordial. So this is basically the loop — a preference model that's being trained separately with this Constitution, and then that's what is being used to fine-tune the LLM to get better. So instead of the RLHF loop, where you had a lot of human feedback, now you're

**[52:18]** using this preference model, which is trained with the Constitution. Yes. *So — all constitutions, maybe there may be some amendments. If so, how do you make sure that you're cost-efficiently updating the constitution for this model, without having to train all over again? And if so, how do you make sure that the previous rules are removed completely?* So there are two answers to your question. One answer is that [Ed: unclear — captions read "AI", marked low-confidence] is generally done as the last stage of the model. And typically, once you have a pretrained base model, post-training has typically been a much smaller percentage of compute — so maybe 5% — and that happens fairly frequently, for models to get updated.

**[53:04]** So, in that sense, if you do think that the Constitution should be updated, that happens at a certain frequency. Overall, I think the question you're asking is this notion of continual learning, and how do we get the models to adhere to certain behavior. I think that's an open research problem at this point in time — how do we get the models to forget a certain set of knowledge, or to adhere to a new set of knowledge, or to follow a new set of rules? That's definitely something. There are interesting answers to that problem, where you can do some canceling — getting it to forget a certain set of knowledge — by looking at, say, interpretability methods, and so on, of, like, can you cancel that knowledge? But then it's not proven that you can get the models to forget something.

**[53:49]** So, in terms of how this scales — this is the number of training sequences they're using, and on the x-axis, they're showing the score of helpfulness Elo. Elo means that humans prefer it in terms of helpfulness. And then, similarly, on the right side, they're showing harmlessness Elo, where humans prefer it in terms of it being more harmless. So now that we have replaced human preferences with AI feedback, we are comparing it to, say, just using helpfulness-based RLHF, or helpful-plus-harmlessness-based RLHF, which is all human-feedback-based, and then just using Constitutional AI, and Constitutional AI with chain of thought. And what they roughly show is that just getting the model to evaluate its responses based on these principles, you still get the model to be equally helpful, maybe

**[54:38]** a little bit less helpful, but it's much less harmless [Ed: so in the captions; the next sentence says the harmlessness scores are much higher] as a result. So the harmlessness scores are much higher, and that was something that the Claude models were extremely strong at. And now that's a set of techniques that are used across the models. Yes. *The Chain of Thought has lower helpfulness Elo.* Say that again. *The Chain of Thought has lower helpfulness Elo.* Yes. *Results [INAUDIBLE]. So it means that Chain of Thought actually makes the performance worse?* Possibly, that's a good point. Is there a measure? And interestingly, on the right, [INAUDIBLE].

**[55:23]** So I think the correlation is less with Chain of Thought and more with the fact that, as I showed you in the last slide — this one — anytime harmlessness is going up, there is some amount of inverse relationship between these two. So it's almost like — in this particular case, if it was more harmless, then it does hurt the helpfulness. But overall, you have to find the right balance between these two. *Thank you.* Yes. *Let me go back to step 1 of Constitutional AI, to the supervised learning part. So it said in the slides that we want to fine-tune on the data generated by self-critique and revision. So I want to understand whether this just simply means that we want to just reinforce the log*

**[56:08]** *likelihoods over, literally, the generations — the rollouts of the L, of the original policy of L. So, basically, we just ask it to critique itself, and then we just fine-tune this sampled rollouts so that they're more likely. That's what this stage is basically [INAUDIBLE]?* It's a stage that's used in a lot of other things that you will see. But what this is basically doing is a set of-- these are traces that it's generated. It's based on reasoning — you want it to follow a certain set of principles, and then it critiqued itself, it revised itself, and it got fine-tuned on this set of traces. I mean, if you were training it for thinking, for example, these would be the reasoning traces.

**[56:56]** So I think being trained on — it's being fine-tuned on those reasoning traces. *But I guess it's interesting that there's not really an explicit feedback--* There is no feedback — because it's just asked to do this thing, and then we just fine-tune it over what it has tried to do. But as long as the fine-tuning is not very large scale — it's much smaller than what the base model was initially trained on — it will not lose its initial capabilities, but the distribution of what it will output will start to get biased towards this set of responses. *So I'm pretty sure the foundation [INAUDIBLE].* The AI feedback stuff? *Yes. How do we evaluate whether or not they provide accurate feedback?* That's a good question.

**[57:41]** So I think for the preference model, when you do train this preference model, you want to have some sort of test or validation set to make sure that the preference model will do a great job — so you do want to have some human input. But typically, what you'll do is a lot of AI feedback. For the preference model, you'll also get it to give some scores on a set of things and then match it with humans. So you do want to do some sort of consistency check with human feedback for the preference model. [INAUDIBLE] It's not the same quality — it's not scaled to 10,000 or that level. You're basically able to do it based on such compositional principles. So overall, in terms of results, what this roughly means is that,

**[58:30]** if you were to plot the harmlessness Elo on the y-axis and the helpfulness Elo on the x-axis, the pretrained base model is basically the raw base model, so it will have a certain set of scores, which are not very high. But with standard RLHF — if you were doing that with just helpful only, you will start to push that frontier towards getting higher scores on helpfulness. And then, if you start to make it less harmless, that's what the orange curve shows you. But then, if you do Constitutional supervised learning, then you're still worse than doing RLHF. But when you do Constitutional RL with chain of thought, that's where you get the frontier — the best Pareto frontier, in terms of the tradeoff between harmlessness and helpfulness. And that's one of the key ideas of this paper — that you

**[59:16]** can strike the right Pareto frontier between helpfulness and harmlessness in this particular case. If you were to generalize this set of ideas across — I mean, generally, anything you want to do in instruction following, where you want the model to follow a subset of instructions but they might be conflicting — then this style of principles applies. And some follow-on work that built on top of that includes the fact that there was another paper that tried to compare RLAIF with RLHF. There was Self-Refine, which talked about iterative refinements with self-feedback. One comment that's worth noting here is that, oftentimes, getting the model to critique itself can be harder, so having a consensus of other models

**[1:00:01]** critique the model sometimes works better, because the models might be overconfident and not know what they know. And then there was another paper that tried to teach models to self-correct via reinforcement learning. So this is an active area of research — just trying to get the models to be better at self-correction, beyond the helpfulness and harmlessness paradigm. So that might be another set of project ideas that you might want to explore. So let's recap, and then we'll close out the class. In terms of the ReAct approach, what we first looked at was this notion of — when we talk to LLMs, we can get them to give model outputs, and what we wanted was for these model outputs to be preferred by humans, which has been a focus.

**[1:00:48]** But what ReAct starts to look at is this notion of, can we get the models to act in the real world? Based on that feedback, and combining that with reasoning, we can get them to be grounded, so they can give better answers to questions, can check facts better. Or if we put these LLMs as decision makers in, say, gaming environments, or in other environments related to, say, web browser agents and so on, then they can make useful decisions. And one interesting aspect of doing it in ReAct style, in language space, ends up being that the decision traces are extremely interpretable. There are several works on top of that, that try to combine this reasoning and acting in different ways, trying to get the models to do better tool calling, to the point where this tool calling is

**[1:01:35]** innate in several models that are out of the box today. But this is the building block of how to get the language models to combine reasoning and acting. A second aspect that we covered today was this notion of RLEF. In coding agents, getting the models to act in the correct way requires some form of execution feedback, and you want to iteratively incorporate that feedback so that you can generate the right code solutions. For code, one of the best ways to get execution feedback ends up being tests, unit tests. So that's one way the models have been used to self-improve, and this also reduces the amount of budget you need to get the models to get better and get state-of-the-art performance

**[1:02:21]** in, say, CodeContests, and other competitive programming tasks. So that's one aspect of just getting execution feedback in the coding domain. And then, finally, what we covered was this notion of Constitutional AI, where we are getting the model to follow a set of constitutions — a set of principles written by humans — to generate feedback for self-improvement. In general, you can also generalize it to anything where you can get the model to follow a set of rules, and then, based on whether it's following the rules well or not, you can get a self-improvement loop going. So hopefully, with this set of papers, you got a sense of how we can build a feedback loop on top of the models. And when the feedback loop has enough signal, then you have a way to improve the model beyond just what is

**[1:03:06]** the data it was trained on. And these were several examples of where this has been shown to work well. Sorry, this slide was not meant to be there. So that would be the end of the lecture, but let's take any questions. OK. *This is a really naive question, but are there-- so, because a lot of human cognitive thinking is very paralleled in how we're designing LLMs, is there actual, very systemic research to combine cognitive behavioral science with LLMs? Or how do we think of new procedures*

**[1:03:53]** *to innovate LLMs like this?* That's a great question. I think, where we are right now, that's what we are — we're using parallels from how humans solve problems, because we are in this space of reasoning LLMs — we're in that era. But I think, at the end of the day, we're still trying to solve the problem of what would get the models to hillclimb without explicitly saying in words what the reasoning space is. So I think the closed-source LLMs have done better than just saying that this is their reasoning space in words. So effectively, what I'm trying to say here is that you can do a lot of techniques

**[1:04:40]** where you can be like, I need to break down the task step by step, which is something that Azalia presented in the first lecture. I need to decompose the task, I need to analyze the task, and send it in parallel loops — that's how humans solve problems. But at the end of the day, you also want to answer the question of, can that whole process of search in the reasoning space be automated, and is there a well-defined process to do that search? If the search space is well-defined, then you can automate it. The challenge of why we're not doing it in an automated way, but in this language space, is because a lot of the tasks that we're giving to these models are not in a well-defined search space.

**[1:05:25]** A good example of that would be that, if you were basically in gaming space, where the action space is well-limited and the search space is well-limited, then you can define models to go figure out what the search space is and how to explore that search space. But if we are talking about experience, and going and experiencing actual environments — whether that's search tools or everything else — you have to collect observations there and then use that. So that's why you're seeing this. *So far, we've been talking about improving LLMs through different techniques. I wonder how those techniques are transferable to improving agents.* You will see that in some of the subsequent lectures. But I mean, all of this--

**[1:06:12]** what's your definition of agents versus LLMs? *Oh, it's basically some LLM that has [INAUDIBLE] and memories and [Ed: unclear — captions read "sketches"; the lecturer's reply mentions sessions and memory] that can carry all of it.* So I think this is starting to represent what I would call an agent, but not with-- it doesn't have sessions, doesn't have memory, because one of the fundamental building blocks of agents is, how do we get the LLMs to respond in a certain way? So this ends up being very useful to understand. *So for frameworks like ReAct, my understanding is that they have some intuition of what kind of [INAUDIBLE] thing calls a reasonable help to the model. But is there a relationship that only [INAUDIBLE] certain domains, and then does it*

**[1:06:58]** *help versus [INAUDIBLE] the other [INAUDIBLE]? So now that we have the more RL-based post-training methods, will this handcrafted framework become obsolete?* Yes and no. The reason the answer is yes is because, if you can define the search space of what to go explore, then yes. No, because defining the search space for all tasks of what to go explore is not well-defined. If you wanted to do the task of an accountant, if you wanted to build a finance agent, if you want to build a legal agent — what is the set of steps they follow? That's very domain-specific. But it's just generating tokens.

**[1:07:44]** [INAUDIBLE] is also just tokens. You're just generating rewards, using the reward to supervise them. So I think, in the abstraction, I can answer it as yes, but the specifics still matter. The reason ReAct as an approach matters is because they are basically saying, OK, I can reason, then I can take action, then I can go reason, I can take action — so it's basically defining the sequence and the workflow. If you remember, from the first lecture, when we defined the agent, we said it's basically some abstraction of how I would define that workflow. So how I would generalize the whole ReAct approach to an agent is, I would say, OK, this is the workflow that I need to follow to accomplish the task that the user has given. But the workflow to accomplish the task is still very domain-specific, so that's why it's very hard to generalize across domains for everything.

**[1:08:34]** Yes. *Well, I have a question about the formalism on slide number 12, the methodology setup page. One question I have — is that there, the way that you define C subscript t, the context — is the context just simply being treated as a state?* Yes. OK. I mean, the reason it's called context is that — I mean, RL state versus prompting is literally what's in the context. *No, I totally agree with that. And then the other thing is, with ReAct, because ReAct has this thought, and then there's this action — but it seems like, in the formalism on page 12, the action — I suppose that action would include, presumably, both the thought and action, because since the action*

**[1:09:20]** *from that thought would just be extracted from the output of the RL.* So, in that particular case, it's being given the choice — it's basically either outputting a reasoning trace, or it's outputting an action, and it's making a decision whether to output one of those two. So it's-- *Oh, I see — so the action can be just like a standalone reasoning trace, and then it stops, and then it does another action.* Yes. *This might be a naive question, but I was just wondering what this harmlessness — have certain features of certain methods structures? So we have [INAUDIBLE] obvious considerations, instead of just trying to sow in the feedback*

**[1:10:06]** *to try to fine-tune the model to somehow improve the harmlessness. For example, what can we do [INAUDIBLE] instead is [INAUDIBLE] can use [INAUDIBLE] to generate certain methods to restrict [INAUDIBLE].* Yes. I think that is done to some extent. At the same time, these models are trained on the internet, so no amount of restriction is really going to get all of that out. So it's a game of — when you are training on a lot of data, that is the tradeoff.

**[1:10:52]** There is definitely some filtering of data that happens. If there are no further questions, we'll call it a close of the class. Thanks, everyone.
