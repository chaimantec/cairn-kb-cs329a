---
title: Part 9 | Future Research Areas
lecture: 9
video: https://www.youtube.com/watch?v=AyO6wyu4DEg
source: edited from auto-generated YouTube transcript
verbatim: false
original: original/09-future-research-areas.md
---

# Part 9 | Future Research Areas — transcript

Copy-edited for punctuation, sentence boundaries and mis-heard terms. This lecture is transcript-only: the course publishes no slides, and the course website lists no readings for it (site row 20, "Future Research Areas"), so terms were restored from context, the course website and this repo's other edited transcripts only. The lecture is given by Aakanksha Chowdhery and Azalia Mirhoseini, who co-present and alternate; their names are confirmed on the course website and in `raw/transcripts/01-course-overview.md` (the captions render Mirhoseini as "Azalea"/"Azalia" and Chowdhery as "Akanksha"). Student questions and comments from the floor are set in *italics*. Timestamps mark the start of each paragraph; all 87 are preserved in order, and no sentence has been moved across a timestamp — where a sentence is split by one, it is left split. `[Ed: …]` markers mark places where the captions are ambiguous, or where a paragraph-internal turn boundary is a best-effort reading rather than a guess. The file ends with the instructors' closing remarks. The verbatim auto-generated captions are kept at `raw/transcripts/original/09-future-research-areas.md`.

**This is an edited transcript.** Restored terms:
- *Aakanksha* (Chowdhery) ← "Akanksha" (≈38:16, 55:39, 58:52) — the course website and `raw/transcripts/01-course-overview.md` both spell it "Aakanksha"; lecture 1's restored-terms list already reads *Aakanksha* (Chowdhery) ← "Akansha"/"Akanksha"
- *Azalia* (Mirhoseini) ← "Azalea" (≈1:39, 39:49, 1:00:23, 1:06:40) — the same speaker's name as in the other edited transcripts; lecture 1's header and lectures 4 and 7's restored-terms lists all read "Azalia"
- *AlphaEvolve* ← "alpha evolve" (≈0:54) — the course site lists "AlphaEvolve: A Gemini-powered coding agent for designing advanced algorithms" in row 7's readings (`sources.md`)
- *AIME* ← "Amy" (≈15:40) — lecture 6 (≈1:39) describes exactly this saturation, "that one [AIME] is already saturated and contaminated in most of the models"; lecture 1's restored-terms list has *hard AIME benchmark* ← "hard aiming benchmark". (Lecture 5 left a similar caption slip, "Amy" at its ≈24:19, as `[Ed: unclear]` — there the surrounding sentence names no benchmark, so nothing settled it; here the sentence is *about* benchmarks and AIME is the one this course has already called saturated.)
- *GSM8K* ← "GSM 8-k" (≈13:22, 14:08) — lecture 1's restored-terms list: *GSM8K* ← "GSM 8-K"
- *pass@1* ← "just past one" (≈20:23) — this course's established notation; lectures 1 and 6 restore *pass@1* ← "pass at one"/"path at 1"
- *best-of-32* ← "best at 32" (≈20:23) — the lecture glosses the term in the same breath ("best-of-32 means you pick the best solution out of 32 generated proofs"); spelled *best-of-N* here rather than *best@N* because that is how lecture 2's transcript writes the same idea, and how the wiki pages write it ($\text{best-of-}N$)
- *KernelBench* ← "kernel bench" (≈38:16) — lecture 2 (≈14:21) names "a data set, or benchmark, on CUDA generation called KernelBench", and the course site lists KernelBench in row 13's readings
- *MMLU Pro* ← "MLU pro" (≈47:46) — the same captioner slip is already restored in lecture 3 (≈59:22): *MMLU Pro* ← "MLU Pro"
- *wet lab* ← "vet lab" (≈35:04) — homophone; the sentence itself is about "running this chemical experiment", which is what a wet lab is for
- *KV caches* ← "KB caches" (≈1:01:59) — the sentence's own contrast is "bringing them into the activations, or in this case into the KB caches"; lecture 4's transcript (≈15:38) spells "KV-cache"
- *the LLMs themselves* ← "the alarms themselves" (≈58:52) — this course's recurring captioner slip, restored in lecture 1 (*these LLMs* ← "these alarms"); the student's own follow-up (≈59:38) says "an LLM or the models themselves"
- *multi-agent fine-tuning* ← "multi-agent finetuning" (≈6:17); *fine-tuning*/*fine-tuned* ← "finetuning"/"fine tuning"/"fine tuned" (throughout) — spelling normalized to the form used in this repo's other edited transcripts

**Unclear passages**, marked inline with [Ed: …]:
- "CNML" (≈20:23) — captions read "benchmarked on IMO problems and CNML problems"; no benchmark of that name appears in the course website's reading lists or in this repo's other transcripts, and the next clause ("the Gemini version of that was presented by one of our guest lecturers") does not name it either
- "1.3 billion" (≈43:00) — the captions read "And in October of this year, it was 1.3 billion", which is lower than the "160 trillion" two paragraphs earlier (≈42:13); kept as captioned, and no material this course's own KB can be checked against resolves it
- "hydrogen token SRS" (≈55:39) — a piece of work from the speaker's own lab, following "My lab did some of this work"; no name of that form appears in the course website's readings or in this repo's other transcripts
- "EICL" (≈1:01:12) — captions read "even a million or a few million models capability to reason over its EICL or in-context kind of data"; the acronym is not attested anywhere else in this course's materials
- "bio disturbance" (≈37:28, student question) — kept as captioned; the speaker's own nearby examples are "scientific discovery" and "this chemical experiment", which do not settle the words
- "into it" (≈45:26) — captions read "especially if we serve the model in quantized versions like into it"
- "fund" (≈41:26) — captions read "even the open source fund for the larger models"; the preceding sentence ("They're proprietary") suggests a different word, but which one is not recoverable
- two unattributed turns — the "Can you hear us? / Yes, we can hear you." exchange (≈36:41) and "Makes sense, yeah. Thanks so much." (≈1:03:34) — are left un-italicized rather than guessed

**Checked, not changed:**
- "TRPO" (≈19:34) — kept as captioned; the captioner spells "PPO" and "GRPO" correctly elsewhere in this course (lectures 3, 4, 6), and this lecture has no reading to check the claim against, so the acronym is left as spoken
- "Professor Ray" and "Professor John Hennessy" (≈39:49) — kept as captioned; a surname is not corrected on a hunch, and the captions give no other reading of it
- "super GPQA" and "natural reasoning" (≈47:46) — kept as captioned; GPQA is this course's established benchmark (lectures 3 and 5), but the captions' forms of these compound names are left alone
- "the absolute zero paper" (≈1:03:34, student question) — kept as captioned, lowercase; the captions give no title styling
- "GP200 and GP300s" (≈45:26) — kept as captioned; the neighbouring accelerators ("H100s and TPUs") do not disambiguate the family the speaker names
- "on a scale of 0.5 and 1" (≈18:01) — the scale the verifier scores proofs on, kept as spoken
- "despite having much human curated data" (≈28:52) — kept as captioned; the next paragraph says they "didn't have any human-curated data on the prompt side", which suggests the captions may have dropped a "not", but the meaning of a captioned sentence is not silently changed here
- "these AI metrics" (≈50:06) and "if it is encoding" (≈24:12) — kept as captioned
- "GPT-OSS", "Gemma3", "Qwen" (≈47:46) — model names kept as captioned

**[0:05]** So we will start with an overview of what we have covered in this quarter. So we started the class with just giving you an overview of LLMs and how over the past year, test-time scaling and train-time scaling using self-improvement techniques, where the fundamental loop is driven by verifiers and feedback from running those verifiers and getting rewards, and then using reinforcement learning and even search algorithms to help the models to climb on math and coding, for example, have been a lot of the focus of initial part of the class. From there, we started talking about evolutionary strategies. So there was a whole class on open-endedness, where we're not just driving self-improvement loop

**[0:54]** with rewards. We also allow the model to just go explore if the exploration space can be defined. And here we discussed AlphaEvolve and similar techniques. Then we moved to a paradigm where we build end-to-end workflows for agents using tool use. And as the models interact with environments and tools, you basically are able to drive the workflows end to end. And if you imagine that if you have tasks that need to be completed over multiple steps, then you do need to have perhaps the ability to look at knowledge bases. So that requires retrieval and memory. So basically, as we go towards real-world workflows,

**[1:39]** we need better planning and multi-step reasoning in these models. So we discussed some works around that and how to really look at evaluations in the next generation of models and what capabilities would drive that. And then we had several guest lectures on post-training and multimodal agents and robotics and reasoning and so on. Azalia, is there anything you would like to add here? Yeah, I think this sounds good. This is full coverage. So one of our guest speakers also talked about the symbolic techniques, which goes under the umbrella of tool use and how that could add to the synthetic data, which is another trend we are seeing.

**[2:25]** Amazing. Thank you. So I think just to recap, and this— the class is named as self-improving agents. So it's not just focusing on LLMs. It's focusing on the agent aspect of it. What that roughly— if we were to remind you of what we discussed in first class, is that how is an agent a generalization of LLM? It has a goal and it will go and interact with the environment, collect feedback, and use that feedback to correct its steps. So they're basically systems that can direct their own processes, use tools, and then accomplish a goal. And oftentimes in the current paradigm, the LLMs by themselves are not powerful enough to drive towards a full goal.

**[3:11]** They're getting there. So oftentimes we would hand-write these systems as workflows where you are orchestrating LLMs and tools. However, in certain scenarios like coding agents, you are seeing some of these agentic workflows being driven by themselves. So it's an exciting time where we are seeing a lot of progress in this area. And how we construct these workflows oftentimes means you're orchestrating LLMs, you're orchestrating verifiers because you need some form of reward. You might have LLMs as judges for verifiers. You might have tool calls. You might have search algorithms in there. So you might have parallel LLM calls that are going and doing these kind of search. But overall, since an agentic workflow is driving towards the goal, it

**[3:57]** needs the capability to plan and to reason over multiple steps, correct itself if it were going in the wrong direction, and overall keep improving in its capabilities, which is where the self-improvement aspect comes in. So in this lecture, as we wanted— we wanted to cover some future research areas. So we organized it as key ideas from certain papers that both cover the self-improvement side of things and just generally the efficiency side of things for intelligence, both of which we believe are extremely important directions in the next research areas. So for the first three papers that I will cover,

**[4:44]** I'll just present the key ideas. But let me just present why those papers were chosen and what are the key ideas that they point in terms of next steps and directions. So what we learned in the class is a lot about, say, test-time scaling, where you could improve by having multiple samples and majority voting and so on, and then train-time scaling, where you can use that feedback in the inference loop and get rewards to drive that self-improvement loop. Oftentimes, this is still limited to narrow domains like math and coding. So how do you get generalization across these domains, and how do you generally get those reasoning chains to be diverse enough so that you can continue

**[5:31]** to drive that self-improvement loop, is something that is an important research area even now. An important aspect of building these self-improvement loops continues to be verification. So how do you have robust verification techniques or meta-verification techniques, where you verify what was going into the reasoning chain, is often quite valuable. So that's another area that the second paper I will present the ideas on will cover. And the third aspect is that even though we talk about train-time scaling, the prompts that go into training these models gets selected very statically and require humans to select them. So how do you break through the data barriers

**[6:17]** so that the self-improvement loop can pick the right set of data that drives that loop? So that's going to be the first part of the focus. And the second part of the focus will be focused on just what drives efficiency for intelligence — so, intelligence per what aspects. So let's take a quick look at some of the ideas that might drive this self-improvement loop further in the next generation of systems. And given how fast the field is moving, maybe we'll be teaching papers on each of this next time. So the first paper that we cover is coming from multi-agent fine-tuning, which focuses on self-improvement with diverse reasoning chains. The key idea of this paper is that pre-training compute,

**[7:05]** and instruction tuning is bottle-necked by— so pre-training compute is a compression of internet scale data, and instruction fine-tuning often requires real human data, where you need human preferences to decide what is good model response and bad model response. So an alternative to this is that you can use the data generated by LLMs, which is often called as synthetic data. And you can do iterative fine-tuning where you generate possible solutions and filter out the incorrect ones. This is called rejection sampling, and fine-tuned only on the good ones. Now there are many variants, STaR and so on, which can help drive this loop even better. We covered STaR in class. You can even add reasoning chains in that process. But what typically happens is that if you're

**[7:51]** using a single large language model to generate the data, then it will generate solutions that will be very similar. And oftentimes the performance increase will stop after a few iterations or after tens of iterations. And typically, this problem comes down to really the notion of lack of diversity. So when you look at model training, at the pre-training scale, the data is so diverse because it was generated over such a long time by humans. So it is quite diverse and that helps the performance. But when a single large language model is generating outputs for a set of prompts, it will not have very diverse responses even at high temperatures. So the intuitive solution for this

**[8:38]** often means that you can use multiple agents, which are specialized in some way, and then use them to improve the diversity. So what this paper proposes, for example, is multiple specialized agents for generation that will then produce diverse initial solutions. So they train generation agents and critic agents. So the generation agents will help generate a lot of diverse initial solutions, while the critic agents will evaluate and refine the solutions. So those are specialists as well. So if you were to look at the step-by-step process — and we don't need to go through every single detail in the algorithm below — the generation agent will come up with an initial answer

**[9:23]** and then there will be debate over multiple rounds. So in the initial step, the generation agent will just have the initial answer, but in the subsequent steps, it will summarize and get a summary of all other agents' responses and use that also as part of generating the next response. The goal of the critic agent is to critique this updated set of answers. So basically, if you're doing any iterations, there's an initial response from each of the generation agents. Then you do a summary across all of these agents, and then the critic agent will critique this updated set of answers. So it's very similar to what you guys were already familiar with. The main difference here is that instead of getting the critique over a single agent's response,

**[10:13]** you're actually using multiple agents' responses and then specializing and summarizing across them. What this enables is that you have some form of diversity, even at the generation stage, before you're critiquing the answer. So you basically get majority voting for free just by having multiple agents and assuming that these are trained slightly differently. You get diversity for free. So the generation models in this particular case are fine-tuned from the same base model. And the goal there is to produce good answers given a question. So over multiple iterations, what they're doing is they're taking outputs and filtering for a match with the majority vote. And then doing SFT on this list of prompt and response pairs.

**[11:00]** If you were looking at a very naive version of doing this, you could just use different models, which is something that people do in practice as well. So they will get very different responses with different prompts. So that's the poor man's version of doing multiple generations. And the critic models in this particular paper were proposed as the critic models that basically take the updated answer and then select the best one. So they are also fine-tuned. And these are fine-tuned on a mix of trajectories where the answer is correct at the start and then is corrected over a course of debate. So basically, the critic model is learning how to contrast the correct and the incorrect

**[11:47]** answer. So to summarize the whole process, there are n generation models and they are each producing an output. The summarization happens. You can imagine that either you summarize it with a model, or you can just concatenate the responses of all the models. Then there is a critique process on that and that's added to the input. And then you do a second round where all of the models generate outputs, updated answers, and then you do majority voting on top of that and again summarize the responses. And you can continue this process in form of a debate in some ways. So those trajectories can then be used to fine-tune different critiques as well.

**[12:35]** So how does this help? Why did we discuss this? So I think the key metric that we're looking for is that on the x-axis, you're looking at how many iterations of fine-tuning it has gone through, and on the y-axis, you either have the negative log likelihood or you have the embedding dissimilarity. The negative log likelihood is just a proxy for performance. And the embeddings dissimilarity, the higher the value, the more diverse it is. So what we see on the left is for two open-source models. When they go and do fine-tuning, they're actually able to continue to increase performance when they do this multi-agent fine-tuning. And Llama seems more responsive to this technique.

**[13:22]** I think the right side is more interesting. As they're going through this process, the responses continue to stay quite diverse. And this is over math as a data set. And overall, they tried this again in math over three open-source models. And since they were going with fine-tuning, they had to stick with open-source models. But with multi-agent techniques, they were able to continue to see improvements even over multiple steps of fine-tuning, while for single-agent fine-tuning, the accuracy collapses or doesn't continue to improve. And they also show that, not just in domain, which is math, in adjacent domains like GSM8K — this is a slightly older piece of work,

**[14:08]** so the numbers are not that high. But even in adjacent domains like GSM8K, the fine-tuned agents actually show higher performance. So this technique actually does help, so it generalizes beyond just in-domain data sets that it's trained on. So that's the first set of comments. So what that roughly says is that if you want self-improvement, the reasoning chains that are provided to the model to drive those need to be diverse in some way. And how you generate that — one technique could be based on having multiple models or multiple agents generate those kind of chains. A second problem that's very challenging, and we covered actually in the first few lectures

**[14:53]** is around verification. So it's quite hard to verify model outputs. And typically, you would just look at the outcome. So if you have a math problem or a theorem proof, what you will look at is the final outcome. And then you have an outcome reward model, so does that match the ground truth, and then you will say that this verifies. One of the questions that we discussed in the verification lecture is that what happens if the reasoning chains are wrong, and will that lead the model astray, or will that cause some performance gap at the end? And we also realize that process reward models are quite hard to build. So DeepSeekMath-V2 is a recent paper that came out which tries to show

**[15:40]** that, at least in the theorem proving domain, it is possible to build self-verification loops, which are automated. So that's a very interesting concept. Just to remind you on what I was saying, the current reinforcement learning approach assumes that the final answer will match the ground truth, and that's how you're basically creating the rewards. So this has enabled saturation of multiple benchmarks like AIME or other math-related benchmarks. But oftentimes, even when you have the correct answer, you might not have the correct reasoning. And if you are actually trying to get the models to do better in theorem proving, you do require rigorous step-by-step derivation, which the final output doesn't quite give you. Now, the interesting aspect is that the large language models

**[16:26]** are often trained on quantitative reasoning, so the proofs that they might generate are mathematically invalid. And if you ask them to verify, they will claim that the incorrect proofs are valid. So LLM as a judge technique doesn't quite work here. But if you actually have expert humans look at these proofs, they will be able to look at the proof and reason about the fact that they know this area. They can reason about the fact and say that, OK, this proof has issues. It's just not making sense because this next step is not following from the last step or there's reasoning gaps in the proof solution. So what DeepSeekMath-V2 proposes is that instead of just training the reward models,

**[17:15]** they train verifiers or meta-verifiers, basically. So they get humans to identify issues in the proofs without any reference solutions. And based on that, they train LLMs to identify issues in the proof and improve upon those. And then if you will find that there— so you basically have trained a model which can now critique the proofs themselves. So what the final architecture for DeepSeekMath-V2 looks like: you folks are already familiar with the fact that there's a generator and a verifier, typically in the loop for building train-time scaling system. So you have one model that is producing proofs and another model that is perhaps verifying things.

**[18:01]** So it's identifying issues. What DeepSeekMath-V2 adds is this notion of meta-verifier. So you have a meta-verifier that will review the verifier's analysis for whether it makes sense or whether there are issues with the proofs. And then the verifier is trained to take these issues and then score the proofs on a scale of 0.5 and 1. So the verifier will then go improve the generator and the generator will produce harder proofs, which will then go improve the verifier. So basically this ends up being a loop in some way. And the other interesting aspect here is that because once you see the data for finding incorrect proofs, then you can actually automate that kind of labeling. So you don't have to just rely on humans identifying

**[18:48]** issues in the proofs once the meta-verifier starts to learn this. So this notion of meta-verification is generally quite interesting in that verifiers can get correct score when the reasoning chains are incorrect. For example, they might come up with fabricated errors. So meta-verification has evaluation of this analysis to these issues that the verifier— verifier here is LLM as a judge — or, do these identified issues actually exist? Does the score follow from the issue? So they're basically analyzing whether the verifier did a good job or not. And then they have experts annotate the quality of this evaluation. And just by adding this additional block,

**[19:34]** they are able to improve the quality. So it's almost like you had reasoning chains and then now you have the verifier over those reasoning chains, which is basically saying that, is this evaluation correct or not? So there's an LLM as a judge, which is identifying the issues in this reasoning chain. And then, do those issues exist or not, is what meta-verification is looking for. If you look at the results here, just with this iterative optimization — I think all of us are familiar that a lot of the RL loop often is built on top of TRPO. In this particular case, they're building on top of DeepSeek-V3-based models. So just the verification generation loop now

**[20:23]** benchmarked on IMO problems and [Ed: unclear: "CNML"] problems — the Gemini version of that was presented by one of our guest lecturers. But this is an open-source version that was released recently. They are able to show that in eight iterations just pass@1, the proof score continues to climb just with that iterative loop. And then if you have best-of-32 — best-of-32 means you pick the best solution out of 32 generated proofs — there you are almost getting to 42% in proof score for IMO shortlist of 2024. So this is quite promising as a hill-climbing technique, which roughly suggests that there is some promise if you can identify

**[21:09]** the issues in the reasoning chains and actually push the model or nudge the model towards correcting them. So overall, the insights here are that the best proofs will achieve higher verification scores, and the generator can learn to differentiate higher quality proofs from flawed proofs. And the self-verification as a result can basically have this better improvement loop. So verification can be a bottleneck and this is one way to break that bottleneck. Overall, I think if you were to try to generalize this technique in other areas, what you're basically looking for is if you have LLM-based verifiers, you need to find a way to get them to identify issues

**[21:54]** without reference solutions. That's what this paper is relying on. Then you need some form of an additional block, which is meta-verification, so that it can reduce the chance of hallucinated issues that were identified by the verifier. And then you add an additional incentive for the generator to maximize quality through deliberate reasoning. So you're basically improving the quality of the model responses here. So overall, this notion of self-verification is introduced by our DeepSeekMath-V2. And it starts to break the bottleneck in verification if you can make that more automated end to end. Of course, this is still limited by domains

**[22:39]** where verification is easier rather than more difficult. So we covered through what is the diversity in reasoning chains and we discussed a bit about the verification bottleneck in the models, where oftentimes to get the models to get over the bottleneck of verification, you would need some form of verification techniques. And DeepSeekMath-V2 publishes that in open source. A third problem that you have to solve is this notion of what data or what prompts should I train on. So there's this very interesting paper

**[23:25]** that has not seen much use yet, but is quite promising and interesting, is that you basically have the models propose the set of tasks that they should go train on that are just at the edge of what they have learned. So to remind you, typically in the current AI training stack, you are either using human-curated reasoning traces, so that's for the supervised learning stage, or for reinforcement learning with verifiable rewards, you are expecting some experts to curate the question-answer pairs, but that basically requires that if you're constructing such a model in math, then you need math experts. If it is IMO problems, then you need IMO experts,

**[24:12]** or if it is encoding, then you need strong software coders. So as the models continue to surpass human intelligence, the ability to find more and more experts and more and more such tasks starts to be limiting. So what this paper proposes, and this idea is still very new, is that a single model can both propose tasks and then solve them. So it almost goes to the other extreme of, we should not really need an external source of data. So basically, we should not need human-generated prompts to climb on. The model itself will propose the tasks and then it will go solve them.

**[24:59]** And so that's a very interesting set of ideas. And it is feasible in certain domains. So they focus more on coding as a domain. And they effectively construct three types of tasks — abduction, deduction, and induction. And I will explain this diagram in the subsequent slides. But effectively, what this is saying is that in the proposal stage, they will construct the tasks based on these task types and then come up with a reward to select which tasks make sense. And in the solution stage, they will verify and then have an accuracy reward. And then that can help give a joint update on the model itself, as opposed to just one-sided updates.

**[25:45]** So what is the proposer? How is the proposer proposing tasks? That's quite interesting. So it's effectively based on coding paradigms. So it's effectively saying that you have deduction tasks where you are generating a program and input and the environment is executing to get output. So this is your usual "come up with a program and an input pair," and then the environment will execute to get outputs. Abduction tasks — they're similar to deduction. You're generating the program and inputs and the environment again computes outputs. But induction tasks are different. It samples an existing program and then generates new inputs for it plus a natural language message describing

**[26:31]** the function, and the environment then goes and executes and decides whether this is going in the right direction or not. And another interesting bit that I should mention here is that the proposer will be conditioned on past here. So I'll cover that in a later slide because otherwise it will get confusing. But it will be basically conditioned on past-generated examples that are explicitly prompted so that they're basically added to promote diversity. So how do you select tasks? The proposer will get a reward based on optimizing for task difficulty. So if when the solver can— so each of the tasks

**[27:18]** that are generated are passed to the solver. And if the success rate is zero, then you get a zero reward. And if the success rate is non-zero, then you basically get 1 minus average success rate. And the kind of task that you want to select are the ones that are not trivial and the ones that are not impossible. So there is some ability to continue to learn. So you end up selecting tasks that are of moderate difficulty, where solver will sometimes succeed and sometimes fail. And this will generally help with getting the model to learn. So that's the goal of the proposer, is to generate tasks for optimal task difficulty at a current set of model weights. And as the model becomes more capable,

**[28:04]** then the proposer should learn to propose harder problems. Of course, if you generate tasks, then you need to somehow make sure that these are valid tasks. So in the coding abstraction, one can run program integrity. So you can actually execute these tasks and see whether there are any errors. You can do safety checks. You can also make sure that if you run these inputs multiple times, you get exactly same outputs because in code that's possible. So the proposed tasks, before they go into the training pipeline, are validated. So that's one way to make sure that the proposal doesn't just hack and come up with garbage tasks. And in terms of— there's a buffer that is kept. So for every seed triplet, there's

**[28:52]** an identity function like basically the input-output and the program for each of those triplet tasks, you're adding them to the task buffer. And the proposal can sample references from that buffer. So there is some sort of a buffer that is kept along with how many times the model is succeeding or failing on this. So this is basically leading to this idea of curriculum learning that is evolving over time. And why is this exciting or interesting? I mean, on coding benchmarks, this notion starts to let you hill-climb despite having much human curated data, which— humans can generate a lot of coding data, but that after a while, synthetic data or some—

**[29:40]** this is almost a play on synthetic data in some ways, but it's generating that in the loop at the task level as well. So what they show is that they're able to get state-of-the-art on coding benchmarks, even though they didn't have any human-curated data on the prompt side. And they are able to outperform models trained on tens of thousands of expert examples. Some emergent behaviors that they show are that the complexity metrics increase over time, so that can be expected if the proposer is continuing to increase the difficulty of what it's learning. And another aspect is that the diversity of programs and answers, when the loop is set up correctly, is improving. And the proposer is actually increasingly generating more and more difficult tasks

**[30:26]** as the training is progressing. So in some ways, it's almost like game theory, where the proposer and solver are slightly adversarial, but overall, they are helping each other improve in some ways. So this is solving to the bottleneck of which tasks to go train the models on. So if you were to look at takeaways here, I think it's basically going down the path of the task selection problem — on which these models run their self-improvement loop — should not get bottlenecked on human data, because otherwise you are very bottlenecked on which human experts, as long as you can have some form of verification there. And you need some form of ability to do curriculum learning. So this particular technique definitely relies on that.

**[31:12]** And even though they only hill-climb on self-proposed code tasks, they actually see strong performance on both coding and math benchmarks. So that's actually surprising that they are also seeing strong performance on math benchmarks, though one can explain that from other results that have been seen in the field. And overall, larger models get bigger gains relative to smaller models. So this is another idea in the space of improving the self-improvement loop that seems quite promising and continuing to hill-climb in that area. Can I add something on the previous? So I feel like we are seeing the same thing. Like, we learned about SWiRL, the step-by-step RL with synthetic data. I feel like the findings here and there really

**[31:59]** kind of resonate with each other in the sense that synthetic data, like model generating its own training data, can improve the model not only on the task that the generation is happening on but also in transferability. Like in SWiRL, it was on, let's do a multi-hop question answering with retrieval. But then as more synthetic data was generated and RL training was done, the model was getting better at other tool calling, like calling a Python and solving math problems, and vice versa. So it seems like there is this clear kind of repeated trend that we are seeing in terms of synthetic data generation by the model and generalization.

**[32:44]** And the other piece that's interesting, again, that kind of is the same kind of observation, is that larger models seems to be better at absorbing this kind of data flywheel and also generalizing through that, especially when it comes to in SWiRL at least on the RL optimization side, which is an interesting observation. Great, great. Where would you take it from what you learned as well. So I was going to summarize and you can add more bullet points. I think to me, to achieve true generalization and reasoning, we really need that notion of diversity in the reasoning chain. So that was the first set of comments I was making. And how we achieved that continues to be an open problem.

**[33:32]** We need better verification, and the more we can break that verify loop in a way where we are not bottlenecked by humans or tasks that are verified only through human experts, the better off we are in some ways, like building reward models is challenging. And then I think the third is the selection of data itself. There's only so much data that can be curated by humans for what prompts go in. So breaking that barrier is also quite important in next generation of self-improving models. What else you would add there? Yeah, I think, one, just like based on these kind of existing work, something that would be interesting

**[34:18]** is that how much we can push the frontier of this self-improvement only based on the verifiable domains that we can generate synthetic data and bring it back to the model on diverse tasks, on diverse, verifiable tasks, how much that pushes the performance and generalizes to other domains when we don't have automated verification. Like, can we generally make the model smarter and smarter in areas that we can verify and minimize or remove the need for labeled data in non-verifiable domains based on that? So I think that would be an interesting research

**[35:04]** area, of course, a very compute-heavy research experiment. There are some questions that people are asking — what are some other applications of AI that don't fall into verifiable problems? So it comes in— I can just think about some areas that verification is very slow, like scientific discovery, running a very slow simulation in chip design that takes like a few days to run to collect one kind of reward signal, or running this chemical experiment that you actually need to go to a wet lab and run things and see what the output, the quality or the reward is.

**[35:53]** Those are the domains that the verification is really hard because in RL fine-tuning, or in test-time scaling, we need these verifiers to be almost instant, or we can wait a little bit. Maybe we can wait minutes. Maybe we can allow one hour. But in this RL training, we need like hundreds or thousands of steps of iteration, we can't wait like days or someone human in the loop to collect the reward for us. So those are the non-verifiable domains — those are some examples of non-verifiable domains. I think truly non-verifiable also comes from creativity or any metrics where there's subjectivity

**[36:41]** in some ways. Like creative writing, it's like designing an exact reward function for that is hard. But of course, we can always model that. But then the RL or the agent can do reward hacking if the model is slightly off base. Any other questions from the class on this aspects? Yeah, we have one question. Can you hear us? [Ed: unclear turn boundary] Yes, we can hear you. *You mentioned the harder areas to verify, like how are people attempting to sort that?*

**[37:28]** *For example, like chip design or [Ed: unclear: "bio disturbance"]?* So one way to go about it is to train a model — like a separate reward model that can predict the outcome of a simulation. So instead of actually running those expensive simulations or emulations or through experiment, you collect a lot of data on them offline, and then you train a reward model that, for a given input, it can predict the quality of the output, and you use this reward model as your verification object, like in the loop of RL optimization and so on.

**[38:16]** But of course, the generality of the reward model is a function of how much data you have. And it can be inaccurate and that can cause problems. So that's one area. Aakanksha, do you have any? So I think we talked about chip design and so on. But even before going to chip design, just the KernelBench stuff that was done at Stanford in your lab — even there, for example, you get compiler execution. So you can see that, did you generate the right code or not? You can get some metrics. But there's a class project that is actually trying to read performance profiles as you make things more complex. And that's a harder problem. And so you basically, even though you might be able to do design-optimized kernels

**[39:03]** for simple enough things, but if you actually concatenate all of these things together and you want to performance profile and change parts of the code that are not optimized, that's a harder problem in some ways. And oftentimes the way people will go about solving it is, OK, the models can solve maybe the kernel optimization problem. So maybe I break down the problem into subparts and then get the model to look at each part and then have some reference solutions to look at — a knowledge base. I'm literally describing a class project right now. So what I'm trying to say here is that because of this being a hard-enough problem, and the models not being at that capability, often it requires breaking down the problem in interesting ways

**[39:49]** to what the models are able to do now. Let's move to— more along the lines of, OK, we can do the self-improvement. But you're now doing a lot more inference. And there's a cost to this intelligence. So Azalia will cover more on how to get efficiency there. Sounds good. Should I present myself or would you scroll? Like what would— Whatever is fastest, I can continue to. If you just tell me next, I'll continue. OK, sounds good. So next, please. So this is a recent work that we did in collaboration with Professor Ray and Professor John Hennessy

**[40:37]** and a large team of great collaborators. So in this project, what we are looking for is like looking into the trends that are happening on the model side and also on the hardware accelerator side. And what we are interested in thinking about the future and thinking about how the workloads and AI inference is going to look like in terms of the type of the size of the models and the type of accelerators that we are going to need to serve them. Next, please. So right now, we are in the mainframe era,

**[41:26]** meaning that the LLMs that we are running are using ChatGPT, Gemini. A lot of these larger models that we are running, all of them are being run on cloud. We don't run them locally because these models are large. Some of them we even don't have access to them. They're proprietary, and even the open source [Ed: unclear: "fund"] for the larger models, we are still using cloud to run these models. And at the same time, what the observation is that the demand for compute as a result is exploding. Like Google Cloud grew in something in the order of 1200x in the last 20 months in terms of the compute serving.

**[42:13]** NVIDIA had a 10x year-over-year kind of growth. And this is crazy. These numbers are explicitly driven by AI. And as a result, we are going to need something in the order of 250 gigawatt of data centers to be able to serve this demand right now. And again, this is exploding. So that means you're going to need more and more energy kind of supply for these data centers. On the right side, we're showing this graph of the number of tokens that are kind of from on the Google side has been processed. So in the February of last year, it was 160 trillion.

**[43:00]** And in October of this year, it was 1.3 billion. [Ed: as captioned — this is lower than the 160 trillion two paragraphs earlier] So this is one of the fastest growing compute demands that we are seeing in history. Next, please. At the same time— so we looked into what kind of workloads are, or the type of activities or AI serving demand that we are observing. And then from this data set, this very large-scale data set of ChatGPT users, it turns out that something in the order of 77% of requests are for task like practical guidance, information — like asking for information or writing.

**[43:47]** And a lot of these tasks, it turns out that we don't need the very best frontier models to answer them correctly; rather smaller and local models can be used to address them. So on the right side, you're seeing this kind of categories of user queries, the type of user queries that exist over time. And what is interesting here is that, of course, users are going to ask more and more complex problems from the chatbots over time because the chatbots are getting better. But still, it's kind of like the vast majority of the type of queries that are being asked are on the side of, again, the simpler side of complexity that

**[44:37]** can be addressed by smaller models rather than large proprietary models. Next, please. Another trend that is happening right now is the improvements in local inference accelerators. So the graph on the right, what it shows is that since 2012 up until now, we saw something like 126x improvement in the GPU memory of the local accelerators. And right now, we have laptops that— our MacBooks could have something in the order of 100 gigabyte of memory. And that means that we can fit very, very large model,

**[45:26]** especially if we serve the model in quantized versions like [Ed: unclear: "into it"]. And so the larger some of the largest models that are out there, we can take them and serve them locally, which is a very interesting trend that is following this other trend that a lot of user queries are addressable by smaller models. Next, please. So the question that we wanted to answer here is that, what role can local inference play in redistributing the inference demand? So we have all this traffic right now that pretty much all of it is going to cloud. Pretty much all of it is going to these accelerators like H100s

**[46:13]** and TPUs and GP200 and GP300s. But the trends right now are suggesting that maybe we can do something different here. And so let's see the next slide. So in order to look into this more systematically, we first define this new metric that looks into not only the capability or the accuracy of the models, but also on the efficiency side. So on the capability side, we are looking at this metric that I'm about to define, called intelligence per watt — is looking into the percentage of the queries

**[46:59]** that are addressable by the model for the single turn and reasoning type of queries. And when I say local models right now, at least in this study, we considered those are the models that have 20 billion active parameters or less. And on the efficiency side is how much useful compute we can get from these per watt from running these models on our local hardware. So in short, the definition of intelligence per watt is the average task accuracy divided by the average power draw to solve this task by the model. Next, please.

**[47:46]** And we looked into a variety of models, hardware, workflow type of tasks and evaluation metrics. We looked into more than 20 local models like Qwen, GPT-OSS, Gemma3 and so on. We looked into both enterprise accelerators and local accelerators. The type of workloads we looked into, as you can see here, there were 1 million queries from source from ChatGPT and other reasoning benchmarks such as natural reasoning, MMLU Pro and super GPQA. And we also looked into the evaluation metrics such as accuracy, energy, latency, the compute used and so on.

**[48:32]** And all of these data— so this massive data, we used it to understand the trends, but we are also open-sourcing all of these kind of different metrics across different type of underlying model and hardware sub-trees. Next, please. So here is our findings. It turns out that local models not only are very, very good already, but the trend of their improvement is also very interesting. So since 2023, there was a 3.1x improvement in the accuracy or the portion of the chat queries that they could solve.

**[49:19]** This is very, very fast. And this happened in only two years. And right now, among the queries that I described in the previous slide, they could address something in the order of 88.7% of all these queries. This is a very, very large number. Another observation, which is kind of not very surprising, is that local accelerators, in terms of their efficiency, they lag behind enterprise chips. For example, an Apple M4 Max, it delivers 1 and 1/2x lower intelligence per watt than the B200. And the reason for that is that these B200s are extremely

**[50:06]** optimized to run language model workloads, whereas an Apple M4, of course, is optimized to run these AI metrics, but there are other kind of workloads that are out there that they're optimized for. And in general, while designing these chips, the understanding wasn't that the LLMs are going to be running locally. That's why the majority of focus of chip designers are on this cloud scale chips for running these workloads. And lastly, there's this other very important observation that the intelligence efficiency, or the improvement in this metric, is something in the order of 5.3x over the last two years.

**[50:55]** So 3.1x of it is coming from better models. Another 1.7x of it is coming from the improvement in the hardware and how the hardware is becoming more and more efficient. So both of these trends — one, is that local models are becoming better and better. They can solve problems that they couldn't solve before at this very accelerated rate. At the same time, model efficiency is becoming better and hardware efficiency is becoming better. This suggests that we are heading towards this future, that more and more of this traffic can be addressed, or can be solved by models that we can run

**[51:42]** on our edge device, for example, on our laptop or on our phone device in the future. Next, please. So let's go the next slide. So with that, I get back to the IPW and the future directions for that in a second. But given this observation and everything else that we have learned in this class, there are a few directions that remain open. There are a lot of interesting research questions around it that are not addressed yet, and it's going to be interesting to work on. One is the foundational principles in test-time scaling

**[52:31]** and learning from this like new kind of era of synthetic data and synthetic data flywheel that these models are creating. Right now, the way we are approaching it is through RL, through collecting this data and then fine-tuning the models. But it kind of like suggests that there is more here, our understanding of, first of all, why are we seeing this kind of property? What does it suggest from the model that as we ask the model a question over and over again through test-time scaling, what is happening that these correct answers are coming out? What are the best practices to distill this successful trajectories back to the model?

**[53:19]** These are still open questions. And there is a very clear segue from this synthetic data flywheel kind of phenomenon, back to continual learning. Something that seems like maybe it's not really— we haven't gotten it right yet, is the following: like us humans, as we solve tasks, as we solve problems and study and do new things, there's this continual kind of progress in how our brain develops and how we become more and more skillful. Whereas for the models, it seems like mostly there's this offline process of here is like some agent take up kind of experiences that are generated.

**[54:05]** And then maybe after some time, there's this fine-tuning process of the model. It is not something that happens on the go. And there is this kind of a mismatch between human behavior and model behavior that a concept like continual learning could potentially add answer. Like, what are the new practices that we can bring in model development that we can bring these positive experiences and learning from negative experiences and problem solving that the model do back into the model in a more natural way that is different from the current asynchronous,

**[54:51]** like data generation and fine-tuning paradigm? The last piece is the infra for the high throughput, low latency test-time scaling. So again, the way we do test-time scaling, but there is repeated sampling, whether it's these kind of back and forth in terms of updates that we do to the previous generations. We do tool calling. We do this and that. That is very different from the current mainstream chatbot usage, which is just mostly single turn back and forth with the model. And what that means is that there are a lot of opportunities for doing systems

**[55:39]** and inference optimization work for these type of test scaling. My lab did some of this work, like works like [Ed: unclear: "hydrogen token SRS"]. And these things are going to matter again a lot more in the future because these methods are becoming more and more mainstream. So that means we need to have specific ways to deal or optimize the systems and the underlying compute for them. So overall, it seems like pre-training— looking at this figure on the bottom of the slide, pre-training — a lot of interesting work in their discourse, or our series of lectures touched less on that, even though Aakanksha is an expert on pre-training.

**[56:25]** But we mostly focused on post-training and the test-time scaling methods. And there is this new kind of unleashed era of like synthetic data, flywheel, and continual learning that is happening in this kind of connection between the fine-tuning and online learning and test-time scaling that we hope that all of you learned a ton about it. But there's also a lot of interesting unsolved challenges that you can address going forward. Next slide, please. Going back to the IPW metric and the observation about the shift, the possible shift from everything on cloud to a lot more locally also suggests new direction

**[57:14]** in terms of inference serving engines that are hybrid, that we can smoothly route traffic between our local and cloud kind of models and accelerators, depending on the need and the complexity of the resources. The other direction here, again, is new model architectures and kernels that we can use for energy efficient inference, especially on the local accelerators that we have, which this is like area that is much less kind of focused on compared to the cloud accelerators. And the other important piece is that energy

**[58:01]** is going to be the most kind of valuable resource that we have going forward. And metrics such as intelligence per watt and better ways of our understanding of measuring energy and watt and power usage and optimizing for that, again, is going to be very, very important. And that is an area that is less worked on right now in terms of the target metric of optimization. But we expect that it becomes more mainstream and more popular going forward. And I think that's the last slide on the future directions.

**[58:52]** Aakanksha, you are muted. There are some things in chat, so— I'll start with the first question here. *Do you think this is more pertaining to memory systems or the LLMs themselves?* I'm guessing— so whoever asked this question, can you ask it real time, perhaps, to put a little bit more context there, otherwise we're guessing the question. Yeah, happy to. *Yeah, I was just referring to a few slides back when you were talking about that continual learning from failures and success.*

**[59:38]** *Do you think that we're going to see more of that in long-term memory systems and architectures, or what's designed around an LLM or the models themselves, in terms of what's improved and what's worked on?* So I think the continual learning idea that might pertain to long-term memory systems — that would be the human analog of it. But if you're trying to— but before we even go there, even in multi-step reasoning, learning from success or failures or getting the model to keep that skill set and update things in the weights, or in subset of weights in a useful way, can you learn a new skill?

**[1:00:23]** Learn how to learn is an important capability that we basically don't have right now. If you can watch videos, robots, try this. When can you watch videos of how to do something and learn how to do it, as opposed to being given a lot of task demonstrations. Azalia, do you have more to add there? You're muted, if you're— Yeah, I think there are other ways also to bring this knowledge, like obviously in context learning is one way to enable continual learning. One way I think about is imagine we had an infinite context

**[1:01:12]** that the model could perfectly have access to every piece of it and could learn from everything in it. We don't have that. But if we had that, maybe that was one solution to continual learning. Because we could put everything positive and negative in the context, and the model could just remember all of that at the same time and reason over all of that at the same time. But we don't have that. And we go through right now — even a million or a few million models' capability to reason over its [Ed: unclear: "EICL"] or in-context kind of data diminishes. And there are other ways that we learned about cartridges in the span of our lectures.

**[1:01:59]** So that's one other way that we are enabling this long context and in context learning without changing the weights of the model, without fine-tuning the model, but instead bringing them into the activations, or in this case into the KV caches of the model. So all I'm saying here is that there are other ways to think about continual learning without model fine-tuning, one could be by just increasing the effective context length massively. And that could be another approach to long-term memory and continual learning.

**[1:02:48]** *As a follow-up to that, in practice, like in implementation, do you think, what would be easier to do? Constantly, updating the model's weights or updating a memory store?* I think it depends on the application. I mean, of course, you can argue that updating the memory store would be easier. If it's a knowledge base, if what you're— I mean, the simplest, my version of that is that if I could keep a database that the LLM could learn to look at, then I should just go update the database. But what you're really trying to teach the LLM is to the ability to reason over new domains. And oftentimes having a side memory system doesn't quite achieve that.

**[1:03:34]** So that's where updating the weights does the job better. And the example that I was giving in robotics actually is very pertinent there as to no matter how much memory systems you add, the robot which has learned— I mean, the cross embodiment generalization that you were looking at on the lecture on Monday, for example, that doesn't happen if you don't update the weights just by having memory systems. So that's more of a skills transfer problem. [Ed: unclear turn boundary — "Makes sense, yeah. Thanks so much." is unattributed in the captions] Makes sense, yeah. Thanks so much. There's one more question, I think from— what he's saying is that *in the absolute zero paper, the environment seems like the data used for post-training.*

**[1:04:19]** *Is there a paper for agents to self-create environments?* I mean, if you go back to the worlds where there was narrow intelligence, there you could basically create simulations for games. And those were used as environments for toy tasks. The reason environments matter in the current generation are that they are proxies for real-world tasks. So it's not so much that there's a paper for agents to self-create environments. It's more along the lines of what is the set of tasks that you're trying to represent. And if there is an easy way to simulate them, then whether you use agents to create that or software to create that, that's fairly straightforward. But is it a reasonable proxy of how

**[1:05:07]** the model will interact with the real world to get feedback. And for gaming, it's kind of a finite space to explore. So it's easier to represent that in code and have simulations. OK, thank you. More questions from the class? You were saying something? I think up here is a question. We can't hear you because you have to speak up. I think that there are no further questions. OK, awesome.

**[1:05:53]** OK, cool. OK, so let's start by— let's end by thanking the class. It's been a real pleasure teaching you all and creating the content for this class so that you can learn about the latest and greatest set of techniques in the self-improving agents area. It's an evolving area, so anything that we teach now starts to be history by the next time the class rolls around. And yet the basic techniques that you're learning are extremely valuable over time, because a lot of the new stuff still builds on top of the basic techniques. So really grateful to have worked with you all and looking forward to what you do in your projects and your posters.

**[1:06:40]** I'll let Azalia also thank the class. Yes, thank you so much for— the way we created this class is that we were also learning about a lot of things as we were creating the slides and we were preparing the course material, because a lot of these topics are just like so fresh and so new. And we were excited about them and we wanted you to also be aware of them. So thanks for accompanying us in this journey. And we are very grateful to you and we hope that you learned some new skills, you got inspired in some new directions, and we really hope that this is just the beginning for you

**[1:07:28]** to go ahead and do so many more amazing work in your research projects, in your jobs, and in the future in general. So thank you so much.
